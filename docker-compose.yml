services:
  api:
    build: ./api
    ports:
      - "${API_PORT}:8080"

  app:
    build: ./app
    ports:
      - "${APP_PORT}:3000"
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:${API_PORT}
    depends_on:
      - api

volumes:
  postgres_data: